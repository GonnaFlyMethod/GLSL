

void main() {
  // Black
  // gl_FragColor = vec4(0.0);

  // White
  // gl_FragColor = vec4(1.0, 1.0, 1.0, 1.0);

  // Green
  // gl_FragColor = vec4(0.0, 1.0, 0.0, 1.0);

  // Yellow
  // gl_FragColor = vec4(1.0, 1.0, 0.0,  1.0);

  // Blue
  // gl_FragColor = vec4(0.0, 0.0, 1.0, 1.0);

  // Custom
  gl_FragColor = vec4(125.0 / 255.0, 0.0, 0.0, 0.0);
}